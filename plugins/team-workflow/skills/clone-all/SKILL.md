---
name: clone-all
description: Initialize all WorkSpace submodules and summarize the workspace tree. Use when the user asks for 전체클론, /전체클론, or wants every submodule initialized in this repository.
---

# Clone All

1. Run `scripts/clone_all.py`.
2. Show the command result, tree summary, and any missing project directories.

## Script

```bash
python plugins/team-workflow/scripts/clone_all.py
```
